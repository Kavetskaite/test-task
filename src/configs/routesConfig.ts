export enum ROUTES {
  LOGIN = '/login',
  ROOT = '/',
  PROFILE = '/profile',
  DASHBOARD = '/dashboard',
}

export enum ROUTES_ALIASES {
  login = 'login',
  root = '/',
  profile = 'profile',
  dashboard = 'dashboard',
}
