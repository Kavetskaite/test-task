import { Action } from '../../interfaces'
// import { SUCCESS_GET_CURRENT_USER, SUCCESS_UPDATE_CURRENT_USER } from '../actions/actionTypes'

export default function user(state = {}, action: Action) {
  switch (action.type) {
    default:
      return state
  }
}